<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/profiles" method="post" enctype="multipart/form-data">
        아이디 :<input type="text" name="userId" placeholder="아이디"/>
        비밀번호 :<input type="Password" name="Password" placeholder="비밀번호"/>
        이름 :<input type="text" name="name" placeholder="이름"/>
        나이 :<input type="text" name="age" placeholder="나이"/>
        <input type="file" name="userfile" />
        <button type="submit">회원가입</button>
        <button type="submit">(동적 폼 전송)회원가입</button>
    </form>

        <script>
            function dynamicUpload() {
              const form = document.forms["dynamic-upload"];
              const formData = new FormData();
              formData.append("title", form.title.value);
              formData.append("userfile", form.userfile.file[0])
      
              axios({
                method:"post",
                url: "/upload/dynamic",
                headers: {
                  "Content-type" : "multipart/form-data"
                }
              }).then((res)=>{
                const result = document.getElementById("dynamic-upload-result")
                  result.innerHTML = `<img src="/${res.data.src}" width="150"/>`
              })
            }
          </script>
      </body>
</html>